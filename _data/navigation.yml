header:
  - titles:
      # @start locale config
      en      : &EN      Resume & CV
      # @end locale config
    url: /about.html

  - titles:
      # @start locale config
      en      : &EN       Projects & Presentation
      # @end locale config
    url: /projects.html

  - titles:
      # @start locale config
      en      : &EN       Courses & Notes
      # @end locale config
    url: /courses.html

  - titles:
      # @start locale config
      en      : &EN       laboratory
      # @end locale config
    url: /laboratory.html

  - titles:
      # @start locale config
      en      : &EN       Awards & Certification
      # @end locale config
    url: /awards.html

  - titles:
      # @start locale config
      en      : &EN       Blog Posts
      # @end locale config
    url: /blog.html

  - titles:
      # @start locale config
      en      : &EN       Consultation & Contact
      # @end locale config
    url: /consultation.html