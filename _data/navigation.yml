header:
  - titles:
      # @start locale config
      en      : &EN      Resume
      # @end locale config
    url: /about.html

  - titles:
      # @start locale config
      en      : &EN       Projects
      # @end locale config
    url: /projects.html

  - titles:
      # @start locale config
      en      : &EN       Courses
      # @end locale config
    url: /courses.html

  - titles:
      # @start locale config
      en      : &EN       laboratory
      # @end locale config
    url: /laboratory.html

  - titles:
      # @start locale config
      en      : &EN       Awards
      # @end locale config
    url: /awards.html

  - titles:
      # @start locale config
      en      : &EN       Blog
      # @end locale config
    url: /blog.html

  - titles:
      # @start locale config
      en      : &EN       Consultation
      # @end locale config
    url: /consultation.html