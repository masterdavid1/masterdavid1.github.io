header:
  - titles:
      # @start locale config
      en      : &EN      Resume & CV
      # @end locale config
    url: /about.html

  - titles:
      # @start locale config
      en      : &EN       Projects & Presentation
      # @end locale config
    url: /projects.html

  - titles:
      # @start locale config
      en      : &EN       Publications
      # @end locale config
    url: /publications.html

  - titles:
      # @start locale config
      en      : &EN       More
      # @end locale config
    url: /more.html
